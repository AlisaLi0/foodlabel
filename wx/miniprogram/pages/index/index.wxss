.hero { padding-bottom: 24rpx; }
.hero-title { font-size: 38rpx; font-weight: 700; }
.hero-sub { margin-top: 12rpx; line-height: 1.6; font-size: 26rpx; }

.picker {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  height: 360rpx; border: 2rpx dashed var(--line); border-radius: 14rpx; background: #fafafa;
}
.picker-plus { font-size: 72rpx; color: var(--brand); line-height: 1; }
.picker-text { margin-top: 14rpx; font-size: 30rpx; }
.picker-hint { margin-top: 16rpx; font-size: 24rpx; text-align: center; }

/* 多图缩略图网格（最多 3 张 + 添加格） */
.thumbs { display: flex; flex-wrap: wrap; gap: 18rpx; }
.thumb {
  position: relative; width: 210rpx; height: 210rpx; border-radius: 14rpx;
  overflow: hidden; background: #fafafa;
}
.thumb-img { width: 100%; height: 100%; display: block; }
.thumb-remove {
  position: absolute; top: 6rpx; right: 6rpx; width: 44rpx; height: 44rpx;
  border-radius: 50%; background: rgba(0,0,0,.55); color: #fff;
  display: flex; align-items: center; justify-content: center; font-size: 34rpx; line-height: 1;
}
.thumb-add {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  border: 2rpx dashed var(--line);
}
.thumb-add .picker-plus { font-size: 56rpx; }
.thumb-add-text { margin-top: 6rpx; font-size: 22rpx; }

.preview { position: relative; }
.preview-img { width: 100%; border-radius: 14rpx; display: block; }
.preview-remove {
  position: absolute; top: 14rpx; right: 14rpx; width: 56rpx; height: 56rpx;
  border-radius: 50%; background: rgba(0,0,0,.55); color: #fff;
  display: flex; align-items: center; justify-content: center; font-size: 40rpx;
}

.steps { margin-top: 28rpx; display: flex; flex-direction: column; gap: 18rpx; }
.step { display: flex; align-items: center; gap: 16rpx; opacity: .5; }
.step.active, .step.done { opacity: 1; }
.step .dot {
  width: 44rpx; height: 44rpx; border-radius: 50%; background: #e5e7eb; color: #9aa0a6;
  display: flex; align-items: center; justify-content: center; font-size: 24rpx; font-weight: 600;
}
.step.active .dot { background: var(--brand); color: #fff; }
.step.done .dot { background: var(--brand); color: #fff; }
.step .lbl { font-size: 28rpx; }

.status { margin-top: 24rpx; font-size: 26rpx; }
.submit { margin-top: 28rpx; }
.credits { margin-top: 18rpx; text-align: center; font-size: 26rpx; }

.tips-title { font-weight: 600; margin-bottom: 12rpx; }
.tips-line { line-height: 1.7; font-size: 24rpx; }

/* 隐私授权弹窗 */
.privacy-mask {
  position: fixed; inset: 0; background: rgba(0,0,0,.5);
  display: flex; align-items: center; justify-content: center; z-index: 1000;
}
.privacy-box {
  width: 600rpx; background: #fff; border-radius: 20rpx; padding: 40rpx 36rpx 28rpx;
}
.privacy-title { font-size: 32rpx; font-weight: 700; text-align: center; }
.privacy-body { margin-top: 24rpx; font-size: 27rpx; line-height: 1.7; color: #333; }
.privacy-link { color: var(--brand); }
.privacy-actions { margin-top: 36rpx; display: flex; gap: 20rpx; }
.privacy-btn {
  flex: 1; margin: 0; font-size: 28rpx; border-radius: 999rpx; line-height: 2.6;
}
.privacy-btn.reject { background: #f2f3f5; color: #666; }
.privacy-btn.agree { background: var(--brand); color: #fff; }
.privacy-btn::after { border: none; }

/* 识别历史（首页内嵌，分 处理中/处理成功/处理失败） */
.hist-h { font-size: 30rpx; font-weight: 700; margin-bottom: 16rpx; }
.hist-item { display: flex; align-items: center; gap: 18rpx; padding: 16rpx 0; border-bottom: 1rpx solid #f0f1f3; }
.hist-item:last-child { border-bottom: none; }
.hist-thumb { width: 92rpx; height: 92rpx; border-radius: 10rpx; flex: none; background: #f2f3f5; }
.hist-thumb-ph { display: flex; align-items: center; justify-content: center; font-size: 40rpx; color: #9aa0a6; }
.hist-info { flex: 1; min-width: 0; }
.hist-name { font-size: 28rpx; font-weight: 600; overflow: hidden; white-space: nowrap; text-overflow: ellipsis; }
.hist-meta { margin-top: 8rpx; display: flex; align-items: center; gap: 14rpx; }
.hist-state { font-size: 22rpx; padding: 2rpx 14rpx; border-radius: 999rpx; }
.hist-state.s-processing { background: #eef4ff; color: #2079db; }
.hist-state.s-success { background: #e7f6ec; color: #1a7f37; }
.hist-state.s-failed { background: #fdecea; color: #c2362f; }
.hist-time { font-size: 22rpx; }
