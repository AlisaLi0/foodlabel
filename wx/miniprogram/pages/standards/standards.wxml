<view class="page">
  <view class="intro muted">本工具合规判定所依据的现行国家标准与部门规章，均自 2027-03-16 起施行。点「查看 PDF 原文」可在线阅读官方 PDF；点「网页版」复制本工具网页链接，在浏览器打开。</view>

  <view wx:for="{{docs}}" wx:key="id" class="card">
    <view class="d-title">{{item.title}}</view>
    <view class="d-sub muted">{{item.sub}}</view>

    <view class="actions">
      <view class="act-btn primary {{item.loading ? 'disabled' : ''}}" data-id="{{item.id}}" bindtap="onOpenPdf">
        {{item.loading ? '加载中…' : '查看 PDF 原文'}}
      </view>
      <view class="act-btn" data-url="{{item.webUrl}}" bindtap="onCopyWeb">网页版（复制链接）</view>
    </view>
  </view>

  <view class="disclaimer muted">以上原文供合规检查参考，正式文本与最终解释以官方发布为准。</view>
</view>

